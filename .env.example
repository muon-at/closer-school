# Kopier til .env og fyll inn for å koble til Supabase.
# Uten disse kjører appen i DEMO-MODUS med rik norsk mock-data (alt funker lokalt).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Settes som secret i Supabase (IKKE i frontend): brukes av edge function functions/coach
# supabase secrets set ANTHROPIC_API_KEY=sk-ant-...